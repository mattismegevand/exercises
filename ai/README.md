# AI

Notebooks implementing various models / algorithms:

- Models:
  - LeNet (mnist)
- Algorithms:
  - Multiple linear regressions (titanic)
  - k-means (iris)
  - k-nearest neighbors (iris)
  - Recommandation (Matrix factorization) (movie_ratings)

- TODO:
  - Models:
    - Resnet (cifar10)
  - Algorithms:
    - Decision tree and Random Forest (golf)

When implementing the "classics" algorithms I try to do so from scratch (NumPy + Pandas).
